# 健診改善方案 — 階段交接（2026-05-06）

> 本檔說明 Wave 1 已完成的事、Wave 2-4 待處理項目、以及如何在另一台電腦繼續處理。

## TL;DR — 一段話交接

Wave 1（文件瘦身與目錄整理）已全部完成。Wave 2（元資料補完 + PDF 殘留修復）、Wave 3（腳本重構與通用化）、Wave 4（前端與部署落地）待續做。原 plan 檔位於 `C:\Users\actotf\.claude\plans\md-token-enumerated-kay.md`，本檔為「真正的執行交接」，應視為續做時的入口文件。

---

## ✅ Wave 1 完成清單

### CLAUDE.md 大手術（825 行 → 511 行 / 97KB → 40KB）

| 動作 | 結果 |
|------|------|
| §0「最新狀態速覽」108 行 → 5 行摘要 + 連結 | 詳情外移到 [docs/_status.md](_status.md)（新檔）|
| §6「視圖架構」135 行 → 27 行 | UI 細節外移到 [docs/04_ui_spec.md](04_ui_spec.md)（既有，新加 anchor 引用） |
| §14「已知擴充點」67 行 → 12 行 | 完整 roadmap 外移到 [docs/roadmap.md](roadmap.md)（新檔）|
| §1「專案目標」45 行 → 18 行 | 校對狀態長段引用 _status.md / data_schema.md |
| §3「資料夾規範」33 行 → 14 行 | 完整資料夾樹引用 README.md |
| §12「決策紀錄」14 行 → 4 行 | 範本移到 docs/decisions.md |
| §17「法源審查紀錄」5 行 → 2 行 | 純導覽 |
| Intro 序言 | 改為「先讀 CLAUDE.md + docs/INDEX.md，其餘按需 fetch」 |

預期效益：每次 session 啟動讀完 CLAUDE.md 由 ~97K token → ~40K token（約 -58%）。

### 新建檔案

- [docs/_status.md](_status.md) — 當前快照 + 歷史 session 摘要（每次 session 結束 append）
- [docs/roadmap.md](roadmap.md) — ✅ 已完成 / ❌ 待做 / 📌 註記項分類
- [docs/INDEX.md](INDEX.md) — 「按需讀文件」目錄；標必讀 vs 按需讀
- [05_scripts/CATALOG.md](../05_scripts/CATALOG.md) — 66 個腳本工具地圖（9 分類 + 待新增清單）
- [05_scripts/_source_dup_audit.py](../05_scripts/_source_dup_audit.py) — 重複檔 audit 工具
- [00_source/_dup_report.md](../00_source/_dup_report.md) — audit 報告（人讀）
- [00_source/_dup_skip_proposed.txt](../00_source/_dup_skip_proposed.txt) — 可 cat 進 _skip.txt 的 EXACT 重複清單

### W1.5 audit 結果（重要）

掃描 787 個來源檔，得到：

- **EXACT 重複 1 群組（3 份檔）**：稿費支給基準數額表 PDF 內容完全相同的 3 份副本，可刪 2 份保留 1 份
- **SIMILAR 重複 46 群組**：每組為「Q 編號相同的長短兩份」（PDF 拆檔換行差異）。pattern 是 ~1KB stub vs ~15KB 完整版。**安全做法**：人工抽看 5-10 組確認 stub 是 truncated 即可批次刪。

完整清單見 [00_source/_dup_report.md](../00_source/_dup_report.md)。

### W1.6（刪檔動作）— **未執行，留給續做 session**

理由：
1. 使用者要求「處理到 Wave 1 結束」並打包後續工作
2. 刪檔屬不可逆（雖有 git 可救），需要使用者親自看過 audit 報告才動手

執行步驟（續做 session 時做）：

```powershell
# 1. 看 audit 報告
notepad 00_source/_dup_report.md

# 2. 確認 EXACT 群組可刪（目前只有 3 份 PDF 的 1 群組）
#    保留命名最完整的，刪另 2 份
Remove-Item "00_source/修正中央政府各機關學校稿費支給基準數額表.pdf" -Confirm
Remove-Item "00_source/支給基準數額表.pdf" -Confirm

# 3. 抽看 5-10 組 SIMILAR 群組，若都是 stub vs full pattern，產出批次刪除清單
#    (可寫個 _apply_dup_skip.py 讀 _dup_skip_proposed.txt 執行)

# 4. git 紀錄
git add 00_source/
git commit -m "chore: rmf duplicate source files per _dup_report.md"
```

---

## ❌ Wave 2-4 待處理（依 plan 順序）

### Wave 2 — 元資料補完 + PDF 殘留修復（2-3 天）

**現況基線**（健診時親自驗證）：
- nodes.json 編碼正常（合法 UTF-8，1106 筆 json.load 成功）
- summary 空缺 **486 筆 (44%)**
- version="TODO" **446 筆 (40%)**
- 無 reviewed 欄位 **194 筆 (17.5%)**
- title 含 `?` 或 CJK 部首區字元 **15 筆**（PDF 抽取殘留，**非編碼問題**）

**待新增腳本**（CATALOG.md 已預留位置）：

1. `_fill_summary_batches.py` + `_fill_summary_apply.py` — 補 486 筆 summary（套既有 _resummary 模式；寬鬆 apply + `summary_review_level: llm` 旗標）
2. `_infer_version.py` — 從 source/body「中華民國 N 年 N 月 N 日修正」推 version；補 446 筆
3. `_backfill_reviewed.py` — 補 194 筆無 reviewed 欄位
4. `_fix_unicode_residue.py` — 對 02_markdown 的 title/summary 跑 `unicodedata.normalize('NFKC', s)`；body 不動
5. `_fix_titles_v3.py` — 整合 v1/v2/_polish 為單一入口（W2.5 + W3.5）
6. `_qa_sample.py` — 隨機抽 20 筆出 markdown checklist 供人工 spot check

**對既有腳本的修改**：
- [01_extract.py](../05_scripts/01_extract.py)、[02_parse.py](../05_scripts/02_parse.py)：加 NFKC 預處理 + title 含 `?` / CJK 部首區字元偵測（W2.6）
- [04_validate.py](../05_scripts/04_validate.py)：新增 completeness 報表（summary/version/reviewed 三欄空缺率），寫入 [03_index/_meta.json](../03_index/_meta.json) 的 `completeness` 欄

### Wave 3 — 腳本重構與通用化（3-5 天）

**最大 ROI**：W3.1 抽 `_llm_batch_base.py` 共用基類，把 16 對「build_batches+apply」的 ~3000 行重複碼收斂到 ~1500 行。具體要 refactor 的對：

- `_retitle_*` / `_resummary_*` / `_llm_review_*`
- `_caveats_gen_*` / `_flow_gen_*` / `_cross_topic_*` / `_source_audit_*`

**其他**：
- W3.2 增量處理 flag（4 主管線加 `--since` / `--changed-only`）
- W3.3 02_parse 結束時呼叫 04_validate --quick
- W3.4 `_cleanup_C.py --parent` 取代國內/國外兩份
- W3.5 `_fix_titles.py` v1/v2/_polish 三層收斂
- W3.6 `_archive/` 實體歸檔一次性腳本（_migrate_* / _split_d001 / _gen_b_files_2026_04_28 等，預估主目錄 66 → 45）
- W3.7 `_pipeline.py` 補完端到端 smoke
- W3.8 _tmp_*.json 30 天 retention

### Wave 4 — 前端與 CF Workers 部署落地（2-3 天）

- W4.1 rate_table 大表外部化：B-國外旅費-003 的 50KB / 150 列 sectioned 表抽出為 `.rates.json` ref
- W4.2 scenarios_manual.json 分層載入（416KB → index 30KB + detail 懶載）
- W4.3 nodes.json 分母題拆檔（worker endpoint `/api/nodes/{parent}`）
- W4.4 search_index.json (1.23MB) 移至 worker
- W4.5 events_worker / data_worker 部署上線（staging 7 天 → prod）
- W4.6 JS 改 ES module（避開 window scope 衝突）
- W4.7 P2 收尾（scenarios_list.json 合一 / rate_lookup KV / Worker ETag）

---

## 🔄 如何在另一台電腦繼續處理

### 方案 A：完整資料夾複製（最簡單）

#### 必須複製的內容

```
支出規定視覺化資料庫\
├── 支出規定視覺化資料庫\          ⭐ 整包複製
│   ├── 00_source\                  唯讀來源（含 _dup_report.md 等本次新檔）
│   ├── 01_extracted\               中間產物（gitignored，但建議帶走）
│   ├── 02_markdown\                ⭐ SSOT，必帶
│   ├── 03_index\                   自動產生（可重 build，但帶走省事）
│   ├── 04_web\                     前端 + scenarios source
│   ├── 05_scripts\                 ⭐ 所有腳本含本次新加的 CATALOG.md / _source_dup_audit.py
│   ├── 06_workers\                 CF Workers code
│   ├── docs\                       ⭐ 含本次新加的 _status.md / roadmap.md / INDEX.md / HANDOFF_2026-05-06.md
│   ├── CLAUDE.md                   ⭐ 已瘦身 511 行版本
│   ├── README.md
│   ├── requirements.txt
│   ├── .git\                       ⭐ 必帶（保留歷史）
│   ├── .github\                    GitHub Actions 設定
│   ├── .gitignore
│   ├── .nojekyll
│   └── (其他根目錄檔)
└── .claude\                        本機 launch.json + settings.local.json
```

**可省略**：
- `.wrangler/` — wrangler 的 local cache，跨機重新 login 即可
- `__pycache__/` 全部 — Python 自動產生
- `.venv/` 或 `venv/`（若有）— 跨機要重新 pip install

#### 建議用 7-Zip 打包（保留中文檔名 + 大幅壓縮）

```powershell
# 在 C:\Users\actotf\CLAUDE\ 執行
7z a -t7z 支出規定視覺化資料庫_2026-05-06_handoff.7z `
  支出規定視覺化資料庫\ `
  -xr!__pycache__ -xr!.venv -xr!.wrangler `
  -mx=9
```

或直接 zip：

```powershell
Compress-Archive -Path 支出規定視覺化資料庫 -DestinationPath 支出規定視覺化資料庫_2026-05-06_handoff.zip -CompressionLevel Optimal
```

預估打包大小（依目前資料體量）：
- 不含 00_source PDF：~50MB
- 含全部：~200-300MB

### 方案 B：只搬 Git + 重新建中間產物

最精省。把 git repo 推到 GitHub（或私 git server）後在新機 clone 即可：

```powershell
# 舊機推
cd 支出規定視覺化資料庫
git remote -v   # 確認 origin
git push origin main

# 新機 clone
git clone <repo url>
cd 支出規定視覺化資料庫
pip install -r requirements.txt
python 05_scripts/03_build_index.py    # 重 build 03_index/
python 05_scripts/_build_scenarios_manual.py --apply
python -m http.server 8765             # 本機驗證
```

**注意**：00_source/ 與 03_index/nodes.json + scenarios_manual.json 不在 git 內（gitignored），需另行複製。

### 方案 C：純文件交接（給人類看）

只帶這幾份就能讓另一個 AI session 接手：

1. `CLAUDE.md`（411 行新版）
2. `docs/INDEX.md`
3. `docs/_status.md`
4. `docs/roadmap.md`
5. `docs/HANDOFF_2026-05-06.md`（本檔）
6. `05_scripts/CATALOG.md`
7. `00_source/_dup_report.md`
8. plan 檔：`C:\Users\actotf\.claude\plans\md-token-enumerated-kay.md`

---

## 🚀 在新機開新 session 接手的開場提示語

把以下訊息貼給 Claude Code（或新 session）：

```
我要繼續執行「支出規定視覺化資料庫」的健診改善方案。

背景：
- 前次 session 完成了 Wave 1（CLAUDE.md 瘦身、新建 docs/INDEX.md、roadmap.md、_status.md、CATALOG.md、跑 _source_dup_audit.py）
- 完整交接見 docs/HANDOFF_2026-05-06.md
- 原 plan 檔在 C:\Users\actotf\.claude\plans\md-token-enumerated-kay.md（若該機器不同位置請對應調整）

請先讀 docs/HANDOFF_2026-05-06.md，然後：
1. 確認 W1.6（刪除 EXACT 重複 PDF 副本 2 份）是否要執行
2. 接續 Wave 2 — 從 W2.1 _fill_summary_batches.py 開始
3. 之後依序 W2.2 → W2.3 → W2.4 → W2.5 → W2.6 → W2.7 → W2.8

執行模式：保持寬鬆 LLM apply + 旗標標示（依使用者先前指示）。每 Wave 結束 commit 一次。
```

---

## 🩹 已知問題 / 注意事項

1. **CLAUDE.md 內舊章節殘留標記**：§14 內留有 `<!-- _ROADMAP_OUTSOURCED_MARKER_ -->` 提示 marker。續做時可清掉（一行 comment）。
2. **`_status.md` 與 `roadmap.md` 內容仍是 2026-05-06 快照**：每次大型 session 結束需 append。
3. **docs/04_ui_spec.md 尚未補 anchor**：CLAUDE.md §6 引用「[docs/04_ui_spec.md](docs/04_ui_spec.md)」但未指明該檔內 chip filter / 抽屜 / scope-banner 的具體 anchor。Wave 4 重構 UI 時順便補。
4. **`_source_dup_audit.py` 的 SIMILAR 邏輯**：目前只比對檔名 Q 編號 + 內容長度差異。Wave 2 後可考慮用 `difflib.SequenceMatcher.ratio()` 加強。
5. **CF Workers 部署狀態**：events_worker.js 程式完整但 `window.EVENTS_ENDPOINT = null`（inert）；data_worker.js 已部署到 `gov-expense-data.ntnick72.workers.dev`。Wave 4.5 才會讓 events_worker 上線。
6. **W2.1 LLM apply 把關**：使用者選了「寬鬆」（直接 apply 加旗標）。Wave 2 各補資料工具務必加 `*_review_level: llm` 或 `*_pending: true` 旗標，方便日後人工抽檢時用 grep 篩出。
7. **要逆轉 Wave 1**：`git checkout HEAD~N -- CLAUDE.md docs/_status.md docs/roadmap.md docs/INDEX.md` 即可（CATALOG.md 與 audit 工具屬新增，逆轉等同刪除新檔）。

---

## 📌 量化驗收基線（健診當下）

供續做時對照進度：

| 項目 | Wave 1 結束時 | Wave 2 目標 | Wave 4 結束目標 |
|------|--------------|------------|----------------|
| CLAUDE.md 行數 | **511** ✅（< 600） | 同 | 同 |
| nodes.json summary 空缺率 | 44% | < 5% | 同 |
| version=TODO 比例 | 40% | < 10% | 同 |
| 無 reviewed 欄位 | 194 | < 20 | 同 |
| title 損傷筆數 | 15 | 0 | 同 |
| 主腳本目錄 .py 數 | 66 | 同 | ≤ 45 |
| 首屏 nodes+scenarios+search 大小 | 2.36MB | 同 | ≤ 400KB |
| events_worker / data_worker 雙線上線 | data_worker only | 同 | ✅ 雙線 |
| 00_source EXACT 重複數 | **1 群組（3 份）已 audit** | 0（W1.6 後） | 0 |
