# ─── 必要依賴 ───

# PDF 文字抽取
pdfplumber>=0.10.0

# DOCX 處理(含表格)
python-docx>=1.1.0

# YAML 處理(front-matter / 索引)
PyYAML>=6.0

# ─── opt-in 依賴(僅特定情境需要,不裝也能跑主管線) ───

# OCR(掃描型 PDF,僅 `python 05_scripts/01_extract.py --ocr` 才載入)
# 模型約 1GB,首次執行會下載
paddleocr>=2.7.0
paddlepaddle>=2.5.0

# OCR 需要把 PDF 轉成圖片
pdf2image>=1.16.0

# ─── 開發工具(可選) ───
black>=24.0.0
ruff>=0.3.0
