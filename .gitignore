# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code 本機設定 / AI 助手指引(僅供本機開發,不對外公開)
.claude/
CLAUDE.md

# 來源原檔(政府公開法規 PDF/MD)— 為精簡 repo 與避免版權疑慮,不納入 GitHub
00_source/

# 資料處理腳本 — 私有程式碼，已遷移至 private repo: ntnick-web/gov-expense-pipeline
05_scripts/

# 中繼抽取產物(可由 02_markdown 重建,但 03_index 是前端載入需要的)
01_extracted/

# OCR 模型快取
.paddleocr/

# 系統檔
Thumbs.db
desktop.ini

# 打包檔
files.zip
*.zip

# 設計交接包(一次性交付物,不進 git)
design_handoff_*/
design_bundle_*/

# 一次性 docx / pdf 報告解壓暫存
_report_unpacked/

# 暫存
*.tmp
*.bak
*~

# Node(若日後加 Node 工具鏈)
node_modules/

# 一次性 LLM batch / audit 產物(已 apply 到 SSOT,不入 repo;2026-05-01 加)
_retitle_proposals/
_resummary_proposals/
_llm_review_proposals/
_*_proposals/
# subagent batch I/O (e.g. _flow_proposals_input.json, _deepen_proposals_output.json)
05_scripts/_*proposals*.json
05_scripts/_*.csv
05_scripts/_*.txt
!05_scripts/_skip.txt   # _skip.txt 不算 dev artefact(00_source 用),保留 — 但其實在 00_source/ 不在這

# 00_source 的「準輸入」配置檔保留(以 _ 開頭但需 tracked);其他 _ 開頭排除
!00_source/_manifest.csv
!00_source/_skip.txt

# 前端開發樣本與 dev 暫存(避免 nodes_sample.json 之類再次進 repo)
04_web/data/*_sample.json
04_web/data/_*.json

# 核心資料已遷移至 Cloudflare KV（gov-expense-data Worker）— 不再公開於 GitHub
03_index/nodes.json
04_web/data/scenarios_manual.json

# Word 暫存鎖定檔(開啟時 Word 會建 ~$ 前綴的鎖)
~$*.docx
~$*.doc
~$*.xlsx

# 內部診斷報告(_gen_review_reports.py 產出,需要時本機重跑;不推公開 repo)
# 規劃文件 _未來優化規劃報告_*.docx 仍 tracked 不受此規則影響
docs/_資料庫*報告_*.docx

# wrangler CLI 快取
.wrangler/
06_workers/.wrangler/
